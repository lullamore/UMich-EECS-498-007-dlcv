# UMich-EECS-498-007-dlcv
record for self learning

in pytorch101, i practiced using pytorch library, I learned how to create and manipulate tensors, improve performance using vectorization, and moving the tensor from cpu to gpu

in knn.py, i implement the k-nearest neighbour model to predict the label on CIFAR-10 dataset. We compare the training speed with and without vectorization. In the end, we use cross validation to find the best k value
